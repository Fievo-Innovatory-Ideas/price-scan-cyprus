# Before publishing — fill these in

Search each file for `[YOUR NAME]` and `PLACEHOLDER` — every spot is marked in the HTML with a comment.

## Required (site looks broken without these)

1. **Your name** — `[YOUR NAME]` appears in the nav, hero, and footer of all three pages
   (`index.html`, `case-study.html`, `backlog.html`). Find & replace.
2. **Personal positioning** (`index.html`, hero) — 2–3 sentences: years of experience,
   domains, what kind of PM you are. A draft is in place; rewrite it in your voice.
3. **Experience highlights** (`index.html`) — 3–4 cards: company, role, one outcome with a
   number each. This is what makes it a *senior* PM site rather than a project page.
4. **LinkedIn URL** (`index.html`, contact section).

## Decisions to make

5. **Product name** — I coined "PriceScan Cyprus" as a working name (the project never had a
   public one). Keep it, or rename — it appears in `index.html`, `case-study.html`,
   `backlog.html` (meta description), and `README.md`.
6. **Live app link** (`case-study.html`, section 08, commented out) — decide whether to link
   the production app. Pro: strongest possible evidence. Con: real user data lives there;
   if you link it, consider a demo account, and make sure the admin area is properly gated.
7. **Screenshots** — the site currently has no product screenshots. It works without them,
   but 3–4 shots (scan review, price-check verdict, cross-store breakdown, admin duplicate
   finder) would strengthen the case study a lot. If you send me screenshots, I'll place them.

## Optional polish

8. A professional photo on `index.html`.
9. A one-page PDF CV linked from the contact section.
10. A custom domain email forward (e.g. hello@yourname.com) once the domain exists.
